# Line comments

Add `//` line comments. A comment starts at `//` and continues to the line ending or end of file. Comment text is ignored by the lexer. The line ending remains a separator under the existing language rules. Block comments are not included.

```text
var count is 5 // Number of iterations
```

Reason: the supplied description does not define comments. This small extension makes sample programs and future tests easier to explain.
